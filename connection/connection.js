(function () {

    // CREATE OBJECT CONNECTION STATIC SINGLE TO SHARE STATUS

    var Connection = {
        status: null // Connection not tested
    };

    module.exports = Connection;

}).call(this);